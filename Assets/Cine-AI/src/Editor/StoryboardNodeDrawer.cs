using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;

public class StoryboardNodeDrawer
{
   
    public static void DrawNode(Vector2 zoomOrg)
    {
       // Rect t = new Rect(m_rect.x - zoomOrg.x, m_rect.y - zoomOrg.y, m_rect.width, m_rect.height);
       // EditorGUI.DrawRect(t, Color.red);
    }
}
